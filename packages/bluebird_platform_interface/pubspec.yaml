name: bluebird_platform_interface
description: Common platform interface for the bluebird plugin.
version: 1.0.0
homepage: https://github.com/chipweinberger/bluebird

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  pigeon: ^27.1.1
